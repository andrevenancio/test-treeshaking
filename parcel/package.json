{
	"name": "parcel",
	"scripts": {
		"parcel:dev": "parcel src/index.js",
		"parcel:build": "parcel build src/index.js",
		"clear": "rimraf dist",
		"start": "npm run parcel:dev",
		"build": "npm run clear && npm run parcel:build",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"devDependencies": {
		"rimraf": "^2.6.3"
	},
	"dependencies": {
		"three": "^0.103.0"
	}
}
